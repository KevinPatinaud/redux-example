# Install Redux with an existed project
If you already have react project create like :

```
npx create-react-app my-app --template typescript
```

You just need to install redux :

```
npm install @reduxjs/toolkit react-redux
```
